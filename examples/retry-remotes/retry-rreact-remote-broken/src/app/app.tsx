// eslint-disable-next-line @typescript-eslint/no-unused-vars
import styles from './app.module.css';


export function App() {
  return (
    <div>
      This is a fallback remote, only used when all else fails
    </div>
  );
}

export default App;
